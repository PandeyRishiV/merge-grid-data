# Backend — Running Instructions

This README describes how to run the backend locally

## Prerequisites
- Node.js >= 18 (or project-specified version)
- npm 


## Install
Using npm:
```
npm install
```

## Run (development)
Start with automatic reload:
```
npm start

```
Server will listen on the configured PORT (default 3000).
